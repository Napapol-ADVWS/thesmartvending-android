<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">3dp</item>
        <item name="android:layout_height">3dp</item>
        <item name="android:layout_marginStart" tools:targetApi="jelly_bean_mr1">20dp</item>
        <item name="android:layout_marginEnd" tools:targetApi="jelly_bean_mr1">20dp</item>
        <item name="android:background">?android:attr/listDivider</item>
<!--        <item name="android:backgroundTint">#FFFFFF</item>-->
    </style>

    <style name="QrImageButton">
        <item name="android:layout_width">@dimen/qrButtonSize</item>
        <item name="android:layout_height">@dimen/qrButtonSize</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="buttonSlotTest">
        <item name="android:layout_margin">@dimen/btnLayoutMargin</item>
        <item name="android:background">@color/buttonTest</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_width">@dimen/btnTestSize</item>
        <item name="android:layout_height">@dimen/btnTestSize</item>
        <item name="android:gravity">center</item>
    </style>

</resources>